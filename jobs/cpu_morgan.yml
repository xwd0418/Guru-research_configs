apiVersion: batch/v1
kind: Job
metadata:
  name: wangdong-cpu-only
spec:
  template:
    spec:

      containers:
        - name: demo
          image: gitlab-registry.nrp-nautilus.io/w6xu/guru-docker-image:3699f97c
          command: ["/bin/bash"]
          # /data/scripts/init.sh;
          args:
            - "-c"
            - >-
              bash /root/gurusmart/MorganFP_prediction/task_scripts/unzip_dataset_v2.sh && step=1 python /root/gurusmart/MorganFP_prediction/reproduce_previous_works/Spectre/notebook_and_scripts/dataset_building/build_infernce_set_db_specific.py && step=2 python /root/gurusmart/MorganFP_prediction/reproduce_previous_works/Spectre/notebook_and_scripts/dataset_building/build_infernce_set_db_specific.py
# bash /root/gurusmart/MorganFP_prediction/reproduce_previous_works/Spectre/notebook_and_scripts/SMILES_fragmenting/build_dataset_specific_FP/save_zip.sh

# bash /root/gurusmart/MorganFP_prediction/reproduce_previous_works/Spectre/notebook_and_scripts/SMILES_fragmenting/build_dataset_specific_FP/save_zip.sh
# /root/gurusmart/MorganFP_prediction/reproduce_previous_works/Spectre/inference/build_deepsat_retrieval_set.py
#step=2 HYUN_FP=1 python /root/gurusmart/MorganFP_prediction/inference_data/build_infernce_set_entropy_mfp.py
              # bash /root/gurusmart/MorganFP_prediction/task_scripts/unzip_dataset_v2.sh &&
             

          volumeMounts:
            # - mountPath: "/data"
            #   name: wangdong-smart-vol
            # - mountPath: "/root/autoencoder_denoiser/exps"
            #   name: wangdong-smart-vol
            - mountPath: "/root/gurusmart"
              name: smart-tess-code
            - mountPath: /dev/shm
              name: dshm
            - mountPath: /workspace
              name: data-unzip
          resources:
            limits:
              memory: 784Gi
              cpu: "18"
              ephemeral-storage: 300Gi
              nvidia.com/gpu: "0"
            requests:
              memory: 784Gi
              cpu: "18"
              ephemeral-storage: 300Gi
              nvidia.com/gpu: "0"


     
      volumes:
        # - name: wangdong-smart-vol
        #   persistentVolumeClaim:
        #     claimName: wangdong-smart-vol
        - name: smart-tess-code
          persistentVolumeClaim:
            claimName: smart-tess-code
        - name: dshm
          emptyDir:
            medium: Memory
        - name: data-unzip
          emptyDir: {}
      restartPolicy: Never
      # imagePullSecrets:
      #   - name: gitlab-auth
  backoffLimit: 0